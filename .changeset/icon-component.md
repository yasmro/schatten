---
'@yasmro/schatten': minor
---

Add `<Icon>` (lv1) — a thin wrapper around lucide-react icons that normalizes
sizing (`sm`/`md`/`lg`) and applies a11y defaults: `aria-hidden="true"` for
decorative icons, and `role="img"` when an `aria-label` is given.
